package com.xmo.music.ui

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import com.xmo.music.R

object XmoFont {
    val logo = FontFamily(Font(R.font.xmo_logo_text))
    val user = FontFamily(Font(R.font.xmo_user_text))
    val bold = FontFamily(Font(R.font.xmo_bold))
    val normal = FontFamily(Font(R.font.xmo_normal))
    val medium = FontFamily(Font(R.font.xmo_medium))
    val thin = FontFamily(Font(R.font.xmo_thin))
}
